### 9

# Eyebrows to neutral position
alter.set_axes([1], [64])
time.sleep(0.2)

# Set pupils to neutral position
alter.set_axes([2, 3], [140, 128])
time.sleep(0.2)

# Open eyes wide
alter.set_axes([4], [0])
time.sleep(0.2)

# Relax cheeks to a content smile
alter.set_axes([5, 6], [255, 255])
time.sleep(0.2)

# Tilt head to neutral position
alter.set_axes([9], [128])
time.sleep(0.2)

# Head up/down to neutral position
alter.set_axes([10], [160])
time.sleep(0.2)

# Head rotate to neutral position
alter.set_axes([11], [122])
time.sleep(0.2)

# Neck nod to neutral position
alter.set_axes([12], [128])
time.sleep(0.2)

# Relax hips to neutral position
alter.set_axes([13], [128])
time.sleep(0.2)

# Relax waist to neutral position
alter.set_axes([14], [128])
time.sleep(0.2)

# Relax abdomen rotation to neutral position
alter.set_axes([15], [128])
time.sleep(0.2)

# Relax left shoulder to neutral position
alter.set_axes([16], [128])
time.sleep(0.2)

# Relax right shoulder to neutral position
alter.set_axes([29], [128])
time.sleep(0.2)